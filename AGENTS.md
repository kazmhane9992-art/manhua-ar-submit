# تعليمات الترجمة (اقرأني أولاً)

هذا المشروع لترجمة فصول المانهوا/المانجا من **الانجليزية (en)** أو **الكورية (ko)** إلى **العربية (ar)**.

## سير العمل

1. يضع المستخدم الفصل في `chapters/en/` أو `chapters/ko/` أو يرسل النص مباشرة.
2. اقرأ `glossary/glossary.json` أولاً قبل كل ترجمة.
3. اقرأ المراجع قبل كل ترجمة (مرجع الأسلوب وقوة الكلمة):
   - `references/style_guide.md` و`references/arabic_style_sample.txt`
   - `references/translation_basics.md` (أساسيات الترجمة والتصنيفات ونبرة الحوار)
   - `references/bubble_symbols.md` (رموز الفقاعات)
   - `references/arabic_punctuation.md` (علامات الترقيم العربية)
   - `references/expressions_and_terms.md` (تعابير ومصطلحات مترجمة غير حرفية)
4. ترجم الفصل بالكامل إلى عربية فصحى ميسّرة بأسلوب المانهوا.
5. احفظ النتيجة في `output/ar/` بنفس اسم الملف المصدر.
6. إذا وجدت مصطلحات جديدة يستحق حفظها، اسأل المستخدم عن ترجمتها العربية ثم أضفها للقاموس.

## القاعدة العليا: الترجمة غير حرفية

> **لا تُترجم الكلمات، تُترجم المعنى والروح.**

- الترجمة الحرفية ممنوعة؛ أعد صياغة الجملة كأنها كُتبت أصلاً بالعربية.
- إذا تعارض الإخلاص اللفظي مع السلاسة، فليكن النصر للسلاسة والمعنى.
- التفاصيل الكاملة في `references/style_guide.md`.

## قواعد الترجمة الأساسية

- **القاموس أولاً**: كل كلمة موجودة في `glossary.json` يجب استخدام ترجمتها المسجلة في القاموس، واتباع أي ملاحظة سياقية فيها، حتى لو بدت ترجمة غير اعتيادية.
- **الأسلوب**: عربية فصحى ميسّرة، طبيعية وسلسة، بأسلوب ترجمة المانهوا المنتشرة (مثل ترجمات مواقع الأنمي والمانهوا العربية).
- **الحوار**: طبيعي ويليق بالشخصية، حافظ على علامات التعجب والاستفهام والانفعال.
- **أسماء الشخصيات**: استخدم الاسم كما هو في القاموس أو الحفاظ على التهجئة الأصلية، ولا تعرّبها عشوائياً.
- **الألقاب الكورية** مثل `형 (hyung)` و`누나 (noona)` و`오빠 (oppa)` و`언니 (unnie)` و`선배 (sunbae)`: انقل المعنى باحترام حسب سياق العلاقة (مثل "أخي الكبير"، "أختي الكبيرة"، "الأخ الأكبر"، "الأخت الأكبر"، "الزميل الأقدم") أو الترجمة المسجلة في القاموس.
- **الاحترام**: استخدم صيغة النداء المناسبة في العربية حسب المقام (لا تُدخل "كيا" أو ما شابه من اللهجات العامية المبالغة إلا إذا طلب المستخدم).
- **المؤثرات الصوتية (SFX)**: اترك الأصل (مثل `쾅`، `드르륵`) مع ترجمتها بين قوسين إن أمكن مثل `(قصف!)`، أو اكتب الترجمة العربية فقط إن كانت واضحة.
- **الجمل غير المكتملة والمركّزة**: حافظ على إيقاع المانهوا، فالكلام المتقطع يبقى متقطعاً.
- **حافظ على فقرات السرد المحددة** ولا تدمجها.
- **الفهم قبل الصياغة**: افهم ما تعنيه الفقاعة أولاً، ثم صيغ ترجمتها. لا تترجم ما لا تفهمه.
- **ضع نفسك مكان المتابع**: هل ستفهم لو كنتَ قارئاً؟ المترجم عامل على الفصل ومتابع في الآن نفسه.
- **الطريقة**: قل العبارة بلغتك الدارجة ثم حوّلها إلى الفصيحة لتسهيل الصياغة.
- **التصنيفات** تحدد درجة ثقل اللغة: الووشيا الخيالي أثقل، والألعاب/نهاية العالم/العصر الحديث أبسط (راجع `references/translation_basics.md`).
- **نبرة الحوار** تتبع الشخصية: الزعماء صارمون، النبلاء هادئون محترمون، الخدم مذلولون ("سموك"، "حضرتكم")، والمقاتلون متكبرون.
- **التمويه في الألعاب**: اختصر إشارات اللاعبين الضمنية ("من الجانب!") بدل فضح الخطة.
- **رموز الفقاعات**: التزم برموز فريق المحررين عند الحاجة (التفكير `( )`، السرد `[ ]`، الصراخ `::`، ملاحظة المترجم `م/`، داخل الصورة `#`، النظام `$`، بجانب الشخصية `@`، فقاعتان متلاصقتان `//`) — راجع `references/bubble_symbols.md`.

## أسماء الملفات

- المدخلات: `chapters/en/<رقم الفصل>.txt` أو `chapters/ko/<رقم الفصل>.txt`
- المخرجات: `output/ar/<رقم الفصل>.txt` (نفس الاسم، مع إضافة رقم الفصل إن لم يكن موجوداً)
- عند الترجمة من الكورية، اذكر للمستخدم الفصل والمصدر (en/ko) والمسطرة الحالية للتأكد.

## القاموس

الملف: `glossary/glossary.json`

لكل مصطلح حقول:
- `term_en`: المصطلح بالانجليزية (إن وُجد)
- `term_ko`: المصطلح بالكورية (إن وُجد)
- `translation_ar`: الترجمة العربية المعتمدة
- `category`: التصنيف (person / place / skill / object / title / sfx / organization / other)
- `notes`: ملاحظات السياق والاستخدام (إلزامي تقريباً — هذا هو "السياق العربي" الذي يمدّه المستخدم)

## إدارة القاموس

يمكن تعديل `glossary.json` يدوياً أو عبر الأداة:

```
python scripts/manage_glossary.py add --en "Solo Leveling" --ar "تسوية الكيان" --cat title --notes "اسم السلسلة"
python scripts/manage_glossary.py search --en Solo
python scripts/manage_glossary.py list
python scripts/manage_glossary.py remove --en "Solo Leveling"
```
